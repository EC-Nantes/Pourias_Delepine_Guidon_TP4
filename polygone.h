/*** POLYGONE H ***/
#include <iostream>
#include <vector>
using namespace std;

template<typename T>

class Polygone {

    private:

    public:
        Polygone();
        Polygone(Vector<Point2D<T>< listeSommets);
        Polygone(Polygone<T> poly);
        

};